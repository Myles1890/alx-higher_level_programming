#!/bin/bash

# Check if the PYFILE environment variable is set
if [ -z "$PYFILE" ]; then
    echo "Error: PYFILE environment variable is not set."
    exit 1
fi

# Print a message indicating which Python script will be executed
echo "Running Python script: $PYFILE"

# Run the Python script using python3
python3 "$PYFILE"

